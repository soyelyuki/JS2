var fecha = new Date();
var year = fecha.getFullYear();
var mes =fecha.getMonth();
var textoHora =`
    el Año es : ${year}
    el mes es: ${mes}
    el dia es : ${mes}`

console.log(fecha);
console.log(Mat.random);
